<header class="topbar">
  <div class="container row-between">
    <a href="/" class="brand">Daare Monitor</a>
    <nav class="nav">
      <% if (currentUser) { %>
        <a class="nav-link <%= currentPath === '/' ? 'active' : '' %>" href="/">Dashboard</a>
      <% } else { %>
        <a class="nav-link <%= currentPath.startsWith('/auth') ? 'active' : '' %>" href="/auth/login">Login</a>
      <% } %>
    </nav>
  </div>
</header>
